% system, network
# change network setting according to proxy
source <bin>

$ bin: which wsl-proxy-sync

% system, process
# kill process forcefully
kill -9 <pss>

$ pss: ps aux | fzf | awk '{print $2}'
