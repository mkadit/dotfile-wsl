% sops, insert, key

# insert key based on gopass age.txt
gopass show <secret> >> ~/.config/sops/age/keys.txt

$ secret: echo $(gopass search age.txt | fzf)

% sops, get, secret

# copy secret value to clipboard
gopass --clip <secret>

$ secret: echo $(gopass list --flat | fzf)

